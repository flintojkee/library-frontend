export enum UserUrls {
  login = 'auth/signin',
  signup = 'auth/signup'
}

export enum BookUrls {
  books = 'books/',
  book = 'books/{id}'
}


export enum OrderUrls {
  orders = 'orders/',
  order = 'orders/{id}'
}
